List of minecraft fabric config libraries.

If you want a config library added (or yours removed), make a PR (In src/java/kr1v/index/libs, copy/paste an existing one, fill in the info, add it to Libraries, and done! The website/json is generated on push). All config libraries are valid!